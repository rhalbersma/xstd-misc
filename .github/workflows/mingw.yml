#          Copyright Rein Halbersma 2014-2026.
# Distributed under the Boost Software License, Version 1.0.
#    (See accompanying file LICENSE_1_0.txt or copy at
#          http://www.boost.org/LICENSE_1_0.txt)

name: MinGW

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow_ref }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
  # canary.yml calls this weekly, so the stub stays callable itself.
  workflow_call:

jobs:
  # No inputs: this library builds on every rung the shared workflow offers,
  # which for MinGW is two -- it drops the development rung with a notice
  # rather than failing, as it does for Apple Clang.
  mingw:
    uses: rhalbersma/cpp-ci/.github/workflows/mingw.yml@8156138fa6336f5d870e72e62b72033632a1c56b # v0.9.0
